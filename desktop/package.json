{
  "name": "open-agent-seal-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "web:dev": "npm --prefix ../open_agent/app/web run dev -- --host 127.0.0.1",
    "web:build": "npm --prefix ../open_agent/app/web run build"
  },
  "dependencies": {
    "@tauri-apps/api": "2.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.5.0"
  }
}
